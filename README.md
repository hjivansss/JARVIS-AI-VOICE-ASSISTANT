commands:
1.setup virtaulenvironment : python 3.12.10 version 
   pip install virtualenv
   virtualenv env312 
   python3.12 -m venv env312
   python3.12 --version
   .\env312\Scripts\Activate.ps1    #activate
   pip install speechrecognition
   pip  install  pyaudio 
   pip install setuptools
   pip install pyttsx3              #text to speech
   pip install pocketsphinx
   pip install google-generativeai
   env312\Scripts\python -m pip install gTTS
   env312\Scripts\python -m pip install google-genai
   env312\Scripts\python -m pip install google-genai
   env312\Scripts\python -m pip install python-dotenv 